import React from 'react';

const GradedLabel = () => {
  return (
    <span
      style={{
        backgroundColor: `#61889E`,
        borderRadius: `2px`,
        textTransform: `uppercase`,
        fontSize: `10px`,
        color: `white`,
        padding: `2px 4px`,
        lineHeight: `15px`,
      }}
    >
      Graded
    </span>
  );
};

export default GradedLabel;
